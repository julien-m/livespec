# Preflight Report

> Generated by `spec.init --from-code` on 2026-04-13.

## Summary

| Check | Status | Notes |
|---|---|---|
| Python 3.11+ | PASS | Python 3.14 in .venv — exceeds minimum |
| pip | PASS | Standard Python tooling |
| Virtual environment (.venv/) | PASS | `.venv/` directory detected |
| livespec CLI (editable install) | PASS | `livespec` binary in `.venv/bin/` |
| ruff | PASS | `.ruff_cache/` detected |
| pyright | MANUAL | Verify with `pyright --version` — may need `pip install pyright` |
| pytest | PASS | Listed in `[project.optional-dependencies.dev]` |
| LLM Provider (optional) | SKIP | Optional — configure if using Layer 4 features |

## Pass 1 — Auto-resolved

No auto-resolvable blockers detected.

## Pass 2 — Human actions required

### pyright

**Action:** Run `pip install pyright` in your virtualenv if not already installed.
**Verify:** `pyright --version`

## Pass 3 — Optional

### LLM Provider

**Action (optional):** Copy `examples/provider-cchub.py` to `~/.config/livespec/provider.py` and configure your LLM backend to enable `--plan-review`, `--semantic`, and `--contradiction-only` features.

---

*Run `/spec.preflight` to re-execute all checks.*
