# Gap Report — Validator CI Prerequisites 079

## Scope and verified local results

The dedicated repair keeps the five original requirements and acceptance obligations. Canonical AC headings now match the native acceptance inventory; no requirement text or check is weakened. Independent SPEC and PLAN reviews are current and progression returned READY before the final assertion edit.

| Requirement | AC | State | Direct evidence |
|---|---|---|---|
| FR-001 | AC-001 | Locally fixed | Both Ruff unions, rejected formatting and all 294 mypy fixture diagnostics repaired; global Ruff, formatting, mypy 627 and pyright 0 pass. All malformed approval fixtures remain exercised. |
| FR-002 | AC-002 | Locally fixed; CI capability pending | Actual pinned Penflow dcbfea6e2e6e6b359b1b0d80607670a4eb7ad6b7 installed from verified private archive. Real positive, malformed and missing-CLI guards pass in the full direct suite. Workflow uses explicit read-only capability, immutable checkout, disabled uv cache and cleanup. |
| FR-003 | AC-003 | Locally fixed | Full direct units: 3726 passed, 26 skipped. Integration3A: 89 passed, 1 skipped, 100 deselected. Node/Chromium dependencies isolated; all 299 prior partitioned node IDs retained exactly. |
| FR-004 | AC-004 | Locally fixed | Existing coverage command: 121 passed, 94.41 percent, unchanged 94 threshold. Added correct/foreign healthy-symlink regression with actual identical target bytes. |
| FR-005 | AC-005 | Unmet | No exact repair-SHA successful primary workflow or two successful observations 120 seconds apart yet. Private Penflow and Anthropic CI capabilities remain pending authorization. |

## Causal repairs and retained boundaries

Taxonomy serializer tests now exercise exact FAIL/exit1 and BLOCKED/exit2 branches, one AST scan, the actual unavailable ESLint diagnostic and serialized taxonomy. A source-bound local catalogue is shared by both consumers; real source-hash validation stays active. The fake AST backend is limited to serializer-unit evidence and proves no AST runtime acceptance. All 8 targeted cases pass.

The SQL reference fixture now says “Use explicit column lists.”, equivalent to the former prohibition of SELECT-star. Existing taxonomy ID, path and domain assertions remain; no negative SQL case was removed. Neutral temporary package.json prevents inherited ESM configuration without forcing CommonJS over TypeScript syntax detection. Exact Chromium 1217 and the real pinned CLI stay in private owned caches.

## Convention compliance and proof limits

Native conventions passes with zero errors; warnings remain visible in its actual receipt. No exclusion or threshold changed. Five large touched test modules are partitioned mechanically through explicit reexports and noncollected support modules. Collection compares 299 original selectors equal with 299 unique results.

Direct successful commands establish local check observations. Native acceptance capture requires a separately prepared, independently reviewed assertion mapping; only AC-002 is eligible for that partial mapping. AC-001, AC-003, AC-004 and AC-005 must not be inferred from it. The immutable spec-fix goal remains active until its remaining actual proofs are accepted; no final feature certification or Implemented promotion is claimed.

## Remaining work

- Complete the reviewed partial native capture and post-fix independent check; retain every exact receipt.
- Inspect the explicit delivery allowlist and staged diff; preserve caches and private dependencies outside Git.
- Obtain authorized CI capability and verify primary workflow plus required checks on the same reviewed SHA twice at least 120 seconds apart.
- Pass independent Manager Validator and Reviewer gates before merge.

## Historical evidence

The pre-fix report and its seven initial structural failures, later structure 5/0 result, original failed runs and complete 39/39 child goal remain preserved in local immutable evidence. They are not rewritten as historical success. Detailed command receipts are retained by CI Guardian; remote proof remains explicitly absent.
