# V5 pre-implementation check — deterministic convention manifest tests

- **Date:** 2026-09-08
- **Result:** **PASS** for a bounded autonomous-test correction; the implementation does not exist yet and is not certified.
- **Scope:** Feature 079 convention-manifest failures only; no source, test, specification, plan, implementation map, changelog, workflow, or private corpus was changed.

## Immutable inputs

| Evidence | Verified value |
|---|---|
| Reviewed Git HEAD | `6dd27b73db2225d1f9a5328346ac29f10e2fb9b7` |
| Guardian packet | SHA-256 `c768b46cea9a2e303b4b67889f91407ac4a0ed45a9148d5055fb45ec97ad0cd3` |
| Failed GitHub run | `34199079760`, from the immutable [failure inventory](/Users/julienm/projects/codex-automation/github-ci-guardian/reports/task-45c889a2/ci-6dd27b7-failures.json) |
| Relevant failures | 11 convention-manifest failures across six test modules; the other two of the 13 run failures belong to Feature 076 |

## Governing requirements

- [`FR-003`](../spec.md#functional-requirements) and [`AC-003`](../spec.md#acceptance-criteria) require the existing full unit and integration suites to pass without disabling tests.
- [`FR-005`](../spec.md#functional-requirements) requires deterministic GitHub validation with exactly `javascript.md`, `rust.md`, and `swift-kotlin.md` privately provisioned at the pinned revision while retaining source-hash validation and full AST tests.
- [`AC-006`](../spec.md#acceptance-criteria) requires those three files before full AST collection, no automatic model/provider/secret dependency, and cleanup preventing private publication.
- The Feature 079 plan step 5 repeats that three-file sparse boundary. Neither the specification nor plan requires GitHub CI to possess or validate 197 private documents.

## Proven gap

These six modules build the decision manifest from `Path.cwd()`:

- [`test_conventions_ars_rules.py`](../../../../tests/test_conventions_ars_rules.py)
- [`test_conventions_css_tailwind.py`](../../../../tests/test_conventions_css_tailwind.py)
- [`test_conventions_generated_catalog.py`](../../../../tests/test_conventions_generated_catalog.py)
- [`test_conventions_source_decisions.py`](../../../../tests/test_conventions_source_decisions.py)
- [`test_conventions_source_family_checks.py`](../../../../tests/test_conventions_source_family_checks.py)
- [`test_conventions_sql.py`](../../../../tests/test_conventions_sql.py)

[`ai_resources_root()`](../../../../validator/conventions_ast/corpus.py) reads `ai_resources_path` from the project manifest and otherwise falls back to `Path.home()/projects/ai-ressources`; it does not select the workflow's `AIRESOURCES` environment variable. The repository [`.conventions/manifest.yaml`](../../../../.conventions/manifest.yaml) has no `ai_resources_path`, so these tests depend on an ambient HOME corpus.

GitHub CI intentionally sparse-checks only the three authorized normative files. The broad manifest tests then see an empty or incomplete corpus and fail through missing decisions, `StopIteration`, and a `0 == 197` count. Even changing production lookup to honor `AIRESOURCES` would leave the broad CSS, SQL, deferred-source, generated-source, and fixed-count assertions dependent on documents that CI is not authorized to expose.

## Minimal correction

1. Reuse the isolated project and synthetic corpus helpers already defined in [`test_conventions_taxonomy.py`](../../../../tests/test_conventions_taxonomy.py): write a temporary `.conventions/manifest.yaml` with explicit `ai_resources_path`, then build the manifest from that temporary project.
2. Extend the declared synthetic fixture only with the representative `code-conventions/general.md` and `code-conventions/tailwind.md` inputs needed by the present ARS, CSS, generated, SQL, and deferred-source assertions. Keep executable, generated, and deferred examples explicit in fixture data.
3. Add `tmp_path` to the 11 affected tests and route only their manifest construction through the isolated fixture. Keep unrelated repository ARS inventory checks on their existing scope.
4. Replace `total_source_count == 197` with the exact declared fixture count, preferably derived from the fixture's source-path set. Retain `decided_source_count == total_source_count`, `undecided_source_count == 0`, and nonzero/exact category checks. Rename the test so its fixture scope is explicit.
5. Assert that the resulting manifest root equals the isolated fixture root, or that decision source paths exactly equal the declared fixture paths. This proves the tests cannot silently read an ambient HOME corpus.

## Assertions that must remain useful

- ARS decisions use rule backends rather than family backends.
- Tailwind/CSS and SQL classifications retain their exact families, capabilities, and anchored reasons.
- Source-decision metadata mutations remain rejected for incomplete executable, generated, and deferred contracts.
- Generated sources still require valid generator evidence; deferred conceptual sources still require an explicit missing capability.
- The fixture catalog is complete and internally consistent, without representing or claiming the private 197-document corpus.

The CI workflow's three-file pinned hash/order/cleanup policy stays covered by its dedicated workflow-policy tests. Full private-corpus validation remains separate local/private evidence. No Anthropic key, model call, provider installation, or code generation belongs in this correction.

## Native full-check status

`livespec validate .specs/features/079-validator-ci-prerequisites --format json` returned exit 0: `spec.md`, `plan.md`, `implementation.md`, `progress.md`, and `changelog.md` each scored 100 with no errors or warnings. System files, feature naming/completeness, the README registry row, orphan-file boundary, and absent optional surface configuration were checked read-only.

| Requirement | Independent status |
|---|---|
| FR-001 / AC-001 | ✅ Mapped by the current implementation report; not rerun in this bounded check. |
| FR-002 / AC-002 | ✅ Mapped to the existing pinned Penflow receipt; not rerun here. |
| FR-003 / AC-003 | ⚠️ Partial: prior full-suite evidence exists, but the reviewed Linux run exposes 11 deterministic-fixture failures. |
| FR-004 / AC-004 | ✅ Mapped to the existing unchanged 94% coverage receipt; not rerun here. |
| FR-005 / AC-006 | ⚠️ Partial: three-file CI policy is mapped, while the six manifest-test modules still depend on the ambient full corpus. |
| AC-005 | ⏳ Pending by design until same-SHA remote stability evidence exists. |

### Convention Compliance

- **Domain checked:** `code`.
- **Sources read:** `$AIRESOURCES/code-conventions/general.md`, `python.md`, `javascript.md`, `cli.md`, and `stack-commands.md` through [`.conventions/index.md`](../../../../.conventions/index.md).
- **Application:** the recommended repair reuses fixtures, keeps tests deterministic and isolated, retains typed production boundaries, and does not weaken assertions.
- **Convention gaps:** none. The proven gap is behavioral test isolation, not a convention-bundle absence or style violation.

## Native goal receipt

| Field | Verified value |
|---|---|
| Goal hash | `8ee91007afe39a7e94bc19a7bba422b7d3ce88b630a68c06ad27fc5d709e4e3e` |
| Contract | `/var/folders/l_/t1s_zytx2fqb0dkzz40d3v7m0000gn/T/livespec-goals/goal-spec-check-8ee91007.contract.json`; SHA-256 `1d50a1a38cd6c6eea5f0c36aefddb4912d49b8a126ab4bfdb53a59439d0e6e0a` |
| State | `/var/folders/l_/t1s_zytx2fqb0dkzz40d3v7m0000gn/T/livespec-goals/goal-spec-check-8ee91007.state.json`; SHA-256 `cc607a8d8090cb8a29f785f38bc5e98117be2e2d29e367e7512e944854bcbfd7` |
| Final status | `complete`; 39/39 tasks complete; 0 pending |
| Run artifact | [spec-check run](../../../.runs/spec-check-2026-09-08T08-24-18.321433-8ee91007-policy2.json); outcome `success`; SHA-256 `edd0886f50d2926207ad5694bf12d67e9ee5f1a8f41ae730afea586a1abad23a` |

The feature changelog now links this V5 check under the amended authority. The pre-existing global Feature 079 spec-check summary and marker satisfied the idempotent global summary task without changing `.specs/changelog.md`.

## Git boundary receipt

- Before the native goal: HEAD matched the reviewed SHA, tracked and staged diffs were empty, and the full status-list SHA-256 was `d54614ca71b66c79a458a311e23e223d0ed5da18a48c1873fbd2140988a8aa17`.
- After the native goal: HEAD remains `6dd27b73db2225d1f9a5328346ac29f10e2fb9b7`; the full status-list SHA-256 is `488bc4d7430e788e6ecfcfb94f7c504c53ad2b5721ab8cf2c68b738c9eb114e7`; the index is empty.
- The only tracked delta created by this goal is the authorized [feature changelog](../changelog.md). The other goal writes are this report, the temporary contract/state pair, and the governed `.specs/.runs/` receipt above. No source, test, normative spec, plan, implementation map, global changelog, commit, or remote state changed.
