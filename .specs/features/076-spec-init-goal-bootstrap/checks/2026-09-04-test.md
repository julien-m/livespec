# Test Report: 076-spec-init-goal-bootstrap

**Date:** 2026-09-04
**Feature:** `.specs/features/076-spec-init-goal-bootstrap/`
**Mode:** full (`--auto --update`)

## Verdict

- **Feature 076:** PASS — 10/10 acceptance criteria have fresh passing evidence.
- **Repository signal:** PARTIAL — the complete no-LLM suite has one pre-existing external-catalog regression, and repository-wide formatting reports six non-feature files. Neither affects Feature 076.
- **Review/remediation tests:** 34 passing cases, including fourteen deterministic descriptor, root, lock, publication, and artifact-ownership race cases from the security reviews.
- **Visual/behavioral:** N/A — this is a non-UI CLI feature with no `## Screens` or `## Behavioral AC` section.

## AC Coverage

| AC | Description | Test | Result | Source | Notes |
|---|---|---|---|---|---|
| AC-001 | Fresh cwd bootstrap render without `.specs` creation | [`tests/test_goal_bootstrap_render.py`](../../../../tests/test_goal_bootstrap_render.py) | PASS | Existing + extended | Exact saved output, state file and hash binding are asserted. |
| AC-002 | Deterministic explicit-target grammar and convention isolation | [`tests/test_goal_bootstrap_render.py`](../../../../tests/test_goal_bootstrap_render.py) | PASS | Existing | Relative/absolute aliases, duplicate and conflict cases covered. |
| AC-003 | Invalid targets fail closed with exit 2 and no writes | [`tests/test_goal_bootstrap_render.py`](../../../../tests/test_goal_bootstrap_render.py) | PASS | Existing | Missing, malformed, unreadable, non-directory and unresolvable targets covered. |
| AC-004 | Prove/archive validate every immutable pairing and status invariant | [`tests/test_goal_bootstrap_pairing.py`](../../../../tests/test_goal_bootstrap_pairing.py) | PASS | Existing + generated | Atomic matrix now includes invalid per-task status for prove and archive. |
| AC-005 | Proof accepts confined evidence and preserves exact task transitions | [`tests/test_goal_bootstrap_prove.py`](../../../../tests/test_goal_bootstrap_prove.py) | PASS | Existing | Pending and complete-task accept/reject transitions covered. |
| AC-006 | Archive writes only through a real contained `.specs/.runs` | [`tests/test_goal_bootstrap_archive.py`](../../../../tests/test_goal_bootstrap_archive.py), [`tests/test_goal_bootstrap_archive_races.py`](../../../../tests/test_goal_bootstrap_archive_races.py), [`tests/test_goal_bootstrap_archive_publication.py`](../../../../tests/test_goal_bootstrap_archive_publication.py) | PASS | Existing + remediation | Success, drift, error, pre-mkdir/root/symlink swaps, pinned traversal, cooperative locking, temporary/final inode ownership, exclusive publication, reopen residue policy, and artifact cleanup cases covered. |
| AC-007 | Missing `.specs` archive and non-init operations remain strict | [`tests/test_goal_bootstrap_archive.py`](../../../../tests/test_goal_bootstrap_archive.py), [`tests/test_goal_bootstrap_non_init.py`](../../../../tests/test_goal_bootstrap_non_init.py) | PASS | Existing | Non-init render/prove/archive stay blocked outside initialized projects. |
| AC-008 | Complete operation/context matrix and initialized-project regressions | Seven feature modules plus [`tests/test_goal_contracts.py`](../../../../tests/test_goal_contracts.py), [`tests/test_goal_archive_cli.py`](../../../../tests/test_goal_archive_cli.py), [`tests/test_run_artifact.py`](../../../../tests/test_run_artifact.py), [`tests/test_version_guard.py`](../../../../tests/test_version_guard.py) | PASS | Existing + generated | 185 feature tests and 179 goal/run/version regressions passed. |
| AC-009 | Control-plane inputs may be external while project paths stay confined | [`tests/test_goal_bootstrap_prove.py`](../../../../tests/test_goal_bootstrap_prove.py), [`tests/test_goal_bootstrap_archive.py`](../../../../tests/test_goal_bootstrap_archive.py), [`tests/test_goal_bootstrap_non_init.py`](../../../../tests/test_goal_bootstrap_non_init.py), [`tests/test_goal_bootstrap_render.py`](../../../../tests/test_goal_bootstrap_render.py) | PASS | Existing + generated | External evidence payload absolute, `..`, symlink and broken-symlink escapes plus render-symlink retargeting are covered. |
| AC-010 | Explicit pair failures and legacy/mixed claims fail with guidance | [`tests/test_goal_bootstrap_pairing.py`](../../../../tests/test_goal_bootstrap_pairing.py) | PASS | Existing | Omitted, empty, malformed, mismatched and legacy inputs covered. |

## Success Criteria Traceability

| Success criterion | Fresh proof | Result |
|---|---|---|
| SC-001 | Seven-module feature suite, 185 passed | PASS |
| SC-002 | [`tests/test_goal_bootstrap_render.py`](../../../../tests/test_goal_bootstrap_render.py) | PASS |
| SC-003 | [`tests/test_goal_bootstrap_render.py`](../../../../tests/test_goal_bootstrap_render.py) | PASS |
| SC-004 | [`tests/test_goal_bootstrap_non_init.py`](../../../../tests/test_goal_bootstrap_non_init.py) | PASS |
| SC-005 | Goal/archive/run/version regression suite, 179 passed | PASS |

## Suite Results

| Suite | Command | Result | Exit | Duration |
|---|---|---|---:|---:|
| Type check | `pyright validator/` | PASS — 0 errors, 0 warnings, 0 information | 0 | 3.52s |
| Lint | `ruff check validator/ tests/` | PASS | 0 | 0.01s |
| Repository format | `ruff format --check validator/ tests/` | PARTIAL — six non-076 files require formatting | 1 | 0.01s |
| Feature format | `ruff format --check` on the 23 Feature 076 production/test and compatibility files | PASS — 23 files already formatted | 0 | 0.01s |
| Feature bootstrap | `python3 -m pytest -p no:cacheprovider -q` on the seven bootstrap modules | PASS — 185 passed | 0 | 1.88s pytest |
| Goal/run/version regression | `python3 -m pytest -p no:cacheprovider -q` on four regression modules | PASS — 179 passed | 0 | 1.75s pytest |
| Complete no-LLM | `python3 -m pytest -p no:cacheprovider tests/ --ignore=tests/integration -q --tb=short` | PARTIAL — 2444 passed, 4 skipped, 1 external-catalog regression | 1 | 59.56s pytest |

## External Regression Attribution

`tests/test_conventions_generated_catalog.py::test_generated_executable_sources_are_explicitly_counted` expects 196 convention sources while the live, externally dirty `ai-ressources` manifest currently contains 197. A fresh manifest probe returned `total_source_count=197`, `generated_executable_source_count=166`, and no catalog load errors. Feature 076 does not modify Feature 073, conventions-AST code, or `ai-ressources`; this finding remains a repository/external follow-up and is not converted into a Feature 076 pass.

Repository-wide format checking separately identified exactly six non-076 files: four conventions tests, one journey test, and `validator/conventions_gates.py`. The exact 23 Feature 076 production/test and compatibility files pass format checking.

## Generation Summary

| Metric | Value |
|---|---:|
| Tests generated or added in remediation | 34 |
| Generated/remediation passing | 34 |
| Generated failing — implementation | 0 |
| Generated failing — compile | 0 |

The first isolated run of the symlink-retarget case archived under the correct immutable root but returned the existing `drift` outcome rather than the test's initial `success` expectation. The assertion was calibrated to that documented archive semantic; the rerun passed and no production defect or production edit resulted.

The final audit's deterministic pre-mkdir `.specs` swap test first failed because path-based creation mutated the attacker-selected external directory. After the descriptor-pinned repair, the external trap remains untouched; a failed operation may retain an empty `.runs` on the pinned original inode because portable cleanup cannot conditionally remove a directory entry.

The second security review exposed three additional RED cases: a contained symlink retarget to the same moved inode archived outside the root, an `O_EXCL` collision deleted another invocation's temporary, and reopen failure left a newly created `.runs`. The fixes now pin both symlink and target identities, track temporary ownership, clean failed post-mkdir reopen, and defer `.runs` creation to the descriptor scope retained through atomic rename; all five race cases pass.

The final security review added two RED regressions for pinned-root traversal and identity-aware cleanup. Archive traversal starts from the retained root descriptor, and cooperative cleanup requests `.runs` removal only with a captured matching inode; all seven race cases pass. The proof-state helpers also use closed JSON mappings plus a typed validation result at the documented legacy Feature 052 boundary.

The final fingerprint review added three RED regressions binding the temporary and final artifact names to the inode opened by this invocation. Within the cooperative-lock model, the writer revalidates that identity around publication and performs identity-aware cleanup; non-cooperative pathname replacement between syscalls remains outside Feature 076, and all ten race cases pass.

The final publication review added two RED regressions for an existing final artifact and a replaced newly created `.runs` directory. The writer now retains and compares the created-directory inode, uses an invocation-unique temporary name, publishes without replacing an existing final name, and performs only identity-aware best-effort cleanup; all twelve archive race/publication cases pass.

The final concurrency review added three deterministic RED checks for pre-capture directory replacement, safe reopen residue, and lock coverage. Confined archive writers hold an advisory lock on the pinned `.specs` descriptor from child creation through artifact cleanup, do not request `.runs` removal after portable creation, and retain the exclusive publication protocol for cooperative writers; all thirteen archive race/publication cases pass.

The final boundary review added seven RED checks: one re-resolves and confines the current `.runs` destination at every identity check, one proves that JSON contract and state closure precede project resolution, and five cover malformed present task shapes for non-init goals. The final compatibility correction preserves the historical unknown-task result for malformed contract task collections while retaining strict malformed-state rejection; all fourteen archive race/publication cases and all seven boundary regressions pass.

The final non-init compatibility review added two RED checks for contract tasks missing `ordinal` or `description`, including when their state task already exists. Eager direct field access now preserves the historical `KeyError` before the existing-state return, while validated bootstrap pairs still establish both fields before state mutation; both regressions pass.

## Evidence Integrity

- Goal hash: `787a8690553bc66e347f523c7bf6f4011507f9d8f136551d61d6b467ecb69891`.
- Final conventions receipt: `.specs/conventions/runs/20260904T142252Z/receipt.json` (PASS, 0 blockers, 110 warnings, 197-source live corpus).
- Finalizer apply receipt: `.specs/features/076-spec-init-goal-bootstrap/run/feature-076-eager-final-apply/finalize/receipt.json` (`applied`); current verify receipt: `.specs/features/076-spec-init-goal-bootstrap/run/feature-076-eager-final-verify-newline/finalize/receipt.json` (PASS, zero violations).
- No additional production file, Feature 052 artifact, pipeline state, hook, ignore file, workflow, or external catalog changed during the final evidence refresh.
