# Gap Report — Feature 010: Visual Testing Complete
**Date:** 2026-04-17  
**Author:** spec.fix

## Summary

| Gap | Type | Status | Fixed |
|-----|------|--------|-------|
| No `playwright.visual.config.ts` created on `--generate` | ❌ Missing | ✅ Fixed | `ensurePlaywrightVisualConfig()` added |
| Test template uses `/TODO-replace-with-actual-route` and `[data-testid="TODO-replace-selector"]` | ⚠️ Partial | ✅ Fixed | Route inference + `page` screenshot |
| No `test:visual:mobile/desktop/firefox/webkit` scripts in `package.json` | ❌ Missing | ✅ Fixed | `ensurePackageJsonScripts()` added |
| No completion report after `--generate` | ❌ Missing | ✅ Fixed | `printCompletionReport()` added |

## Gaps Closed: 4/4

### FR-024/AC-028 — `playwright.visual.config.ts` auto-generation

The migration tool now calls `ensurePlaywrightVisualConfig()` on every `--generate` run. Creates `playwright.visual.config.ts` at project root with 5 projects (mobile/tablet/desktop/firefox/webkit) and `snapshotPathTemplate: 'baselines/{projectName}/{arg}'`. Preserves existing file (AC-030 hard guard).

### Test template improvement

Template no longer contains `/TODO-replace-with-actual-route` or `[data-testid="TODO-replace-selector"]`. Changes:
- Route inferred via `inferRouteFromFeature()` (slug→route heuristics + spec.md URL extraction)
- Mockup comparison test uses `expect(page).toHaveScreenshot()` (full page, not locator-based)
- Added `fullpage layout` test (AC-007)
- Added `responsive viewport` test (AC-012)
- Added Quick Start comment block

### `package.json` scripts

`ensurePackageJsonScripts()` adds missing `test:visual:mobile`, `test:visual:desktop`, `test:visual:firefox`, `test:visual:webkit` scripts. Preserves existing entries.

### Completion report

`printCompletionReport()` outputs a structured next-steps guide after `--generate` completes, including files created, scripts added, and a 5-step workflow.

## Verification

- 11/11 integration tests pass (`tests/integration/test_migrate_visual.py`)
- 478 Python tests pass, 0 failures
- Sentinel format preserved: `VISUAL_SCAFFOLD_RESULT: files=N dirs=M`
- Dry-run does NOT emit sentinel (confirmed)
- Second `--generate` run is idempotent (confirmed)

## Remaining Gaps

None.
