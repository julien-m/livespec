# Spec Fix Report - W51 XCUITest Simulator Ranking Audit Blocker

**Checked:** 2026-06-09
**Feature:** See [`057-cross-feature-user-journeys-v2`](../)
**Command:** `$spec-fix "LiveSpec journey runner simulator ranking audit blocker"`
**Goal hash:** `2dc20b6bf9cdd20957aa042a055f5362c5a33b8687d2be8056a3f7a714e9e426`

## Verdict

**Overall health:** Ranking correction verified by targeted runner checks and orchestration validators.

The W49 runner selected shutdown devices only inside each runtime. That still allowed a booted newest-runtime iPhone to beat a shutdown older-runtime iPhone, and it could allow a shutdown iPad to beat a booted iPhone when state was ranked before family. The runner now builds one candidate list across all matching runtimes and sorts by family priority, shutdown state, runtime rank, device name, then UDID as an implementation-only deterministic tie-breaker not covered by a dedicated regression.

## Corrections Verified

| Gap | Previous Status | Current Status | Evidence |
|---|---|---|---|
| W51 - Shutdown preference was scoped per runtime | Blocking | ✅ Corrected | See [`validator/journeys/runner.py`](../../../../validator/journeys/runner.py) for the cross-runtime candidate ranking. |
| W51 - iPad shutdown could outrank iPhone family | Blocking | ✅ Corrected | See [`tests/test_journey_v2_runner.py`](../../../../tests/test_journey_v2_runner.py) for the booted-iPhone-over-shutdown-iPad regression. |
| W51 - Older shutdown iPhone could lose to newer booted iPhone | Blocking | ✅ Corrected | See [`tests/test_journey_v2_runner.py`](../../../../tests/test_journey_v2_runner.py) for the shutdown-older-runtime-iPhone regression. |
| W51 - watchOS family must stay Apple Watch | Guarded | ✅ Preserved | Existing watchOS runner regression still passes and selects `platform=watchOS Simulator,id=WATCH-11`. |
| W53 - watchOS shutdown state precedence missing | Major | ✅ Corrected | See [`tests/test_journey_v2_runner.py`](../../../../tests/test_journey_v2_runner.py) for the watchOS shutdown precedence regression. |
| W53 - Booted simulator boot path was unrealistic | Major | ✅ Corrected | See [`validator/journeys/runner.py`](../../../../validator/journeys/runner.py) for the already-booted simulator boot skip. |
| W53 - Simulator `state` validation lacked regression coverage | Minor | ✅ Corrected | See [`tests/test_journey_v2_runner.py`](../../../../tests/test_journey_v2_runner.py) for the non-string state rejection regression. |

## Verification

| Check | Status | Details |
|---|---|---|
| Diff hygiene | ✅ Pass | V36 PASS: `git diff --check`. |
| Python lint | ✅ Pass | `ruff check validator/journeys/runner.py tests/test_journey_v2_runner.py`. |
| Python format | ✅ Pass | `ruff format --check validator/journeys/runner.py tests/test_journey_v2_runner.py`. |
| Runner unit suite | ✅ Pass | `pytest tests/test_journey_v2_runner.py -q` -> 27 passed. |
| Targeted pyright | ✅ Pass | `pyright validator/journeys/runner.py` -> 0 errors. |
| Targeted mypy | ✅ Pass | `mypy tests/test_journey_v2_runner.py validator/journeys/runner.py` -> no issues in 2 source files. |
| LiveSpec coherence | ✅ Pass | `livespec validate --coherence` -> 0 errors, 0 warnings, 4 infos. |
| LiveSpec doctor | ✅ Pass | `livespec doctor` -> OK, 0 errors, 0 warnings, 4 infos. |
| Post-fix command-level check | ⚠️ Not run | `$spec-check 057-cross-feature-user-journeys-v2` was not executed in this no-native-agent worker; no command-level PASS is claimed. |

## Remaining Gaps

The W51/W53 ranking defect is corrected and covered by targeted checks. Post-fix
independent verification is the recorded V36 read-only validation for this
no-native-agent worker: diff hygiene, targeted ruff/pytest/mypy/pyright, LiveSpec
coherence, and LiveSpec doctor all passed without launching Xcode, UI, simulator,
or native subagents.

---

# Spec Fix Report - W49 XCUITest Pre-Suite Hang

**Checked:** 2026-06-09
**Feature:** See [`057-cross-feature-user-journeys-v2`](../)
**Command:** `$spec-fix "XCUITest journey runner hangs before Test Suite starts"`
**Goal hash:** `8645c650c062ea676318de1cd2b6fb07343c4886e37fd9e6d7e01015a13a6269`

## Verdict

**Overall health:** Fixed for the W49 pre-XCTest hang.

W48 manually targeted iOS simulator UDID `8C365700-3496-4DEF-BCA8-6D6EFC6C5723`. That device was reported `Booted`, but `xcodebuild test` and `test-without-building` never reached `Test Suite`/`Test Case` and had to be interrupted. A different iOS 26.5 simulator reached `Test Suite` immediately, proving the generated journey Swift was not the pre-suite hang root cause.

## Gaps Closed

| Gap | Previous Status | Current Status | Evidence |
|---|---|---|---|
| W49 - Runner reused a booted but wedged iOS simulator | Blocking | ✅ Fixed | [`validator/journeys/runner.py`](../../../../validator/journeys/runner.py) now records simulator `state` and prefers shutdown available destinations before already-booted devices. |
| W49 - iOS journey autodetection could choose non-phone simulators | Major | ✅ Fixed | iOS destination selection now prioritizes `iPhone` devices over iPad devices before building the `id=<UDID>` destination. |
| W49 - Missing regression proof for stale booted simulators | Major | ✅ Fixed | [`tests/test_journey_v2_runner.py`](../../../../tests/test_journey_v2_runner.py) proves a booted stale iPhone is skipped in favor of a shutdown iPhone. |

## Fix Plan Executed

1. Reproduce W48 with a single iOS UI test on UDID `8C365700-3496-4DEF-BCA8-6D6EFC6C5723`: still timed out before `Test Suite`.
2. Prove another iOS 26.5 simulator reaches `Test Suite` with `STRAPTUITests/STRAPTUITests/testExample`.
3. Patch compiled journey destination selection to prefer shutdown iPhone/watch devices over booted devices, then boot and await the chosen UDID.
4. Add unit coverage for the stale-booted-device regression and iPhone-over-iPad selection.

## Verification

| Check | Status | Details |
|---|---|---|
| Runner unit suite | ✅ Pass | `pytest tests/test_journey_v2_runner.py -q` -> 23 passed. |
| Python lint/format | ✅ Pass | `ruff check validator/journeys/runner.py tests/test_journey_v2_runner.py`; `ruff format --check validator/journeys/runner.py tests/test_journey_v2_runner.py`. |
| Targeted type check | ✅ Pass | `pyright validator/journeys/runner.py` -> 0 errors. |
| Strapt minimal UI control | ✅ Pass | `xcodebuild test-without-building ... id=E8310F4C-9940-4A39-A2D6-FBF98E6D953B ... STRAPTUITests/testExample` reached `Test Suite` and passed. |
| Strapt targeted journey run | ✅ Pass for hang fix | `PYTHONPATH=/Users/julienm/projects/livespec gtimeout 240 python3 -m validator.cli journey run --journey import-workout-to-library --json` reached `Testing started completed` in ~54s and returned explicit `** TEST FAILED **` instead of hanging before XCTest. |
| Post-fix command-level check | ⚠️ Not run | `$spec-check 057-cross-feature-user-journeys-v2` was not executed in this no-native-agent worker; no command-level PASS is claimed. |

## Remaining Gaps

The targeted Strapt import journey still fails functionally after XCTest starts. That is downstream Strapt journey/app behavior and should be handled by a fresh `$spec-fix` worker after this LiveSpec change is committed/pushed and Strapt is re-run against it.

---

# Spec Fix Report - W31/W33/W35/W37 Simulator Destination Corrections

**Checked:** 2026-06-09
**Feature:** `.specs/features/057-cross-feature-user-journeys-v2/`
**Command:** `$spec-fix 057-cross-feature-user-journeys-v2`
**Goal hash:** `2dc20b6bf9cdd20957aa042a055f5362c5a33b8687d2be8056a3f7a714e9e426`

## Verdict

**Overall health:** Fixed for the W38/W39 audit corrections accepted by V22, preserving the V21 W37 simulator-destination scope proof.

The W39 brief states that V21 passed on W37. W38 then blocked `$git-commit` because this report started with W36/W34/W32 finalization claims instead of the W31/W33/W35/W37 scope validated by V21.

## Gaps Closed

| Gap | Previous Status | Current Status | Evidence |
|---|---|---|---|
| W31 - XCUITest journey runner hardcoded unavailable `iPhone 16` destination | Blocking | ✅ Fixed | [`validator/journeys/runner.py`](../../../../validator/journeys/runner.py) resolves available iOS/watchOS simulator destinations via `xcrun simctl list devices available --json` and emits `id=<UDID>`. |
| W33/W35/W37 - validated simulator-destination correction chain | Blocking | ✅ Fixed | W39 brief records `V21 PASS on W37`; this W39 report keeps the claim scoped to that validated chain. |
| W38 audit-documentation mismatch | Major | ✅ Fixed | [`changelog.md`](../changelog.md), [`progress.md`](../progress.md), and [`../../changelog.md`](../../changelog.md) no longer start with W32/W34/W36 finalization claims. |

## Fix Plan Executed

1. Keep the report scoped to W31/W33/W35/W37 validated by V21.
2. Remove W32/W34/W36 finalization claims from the visible top of the W39 traceability artifacts.
3. Preserve the existing W31 simulator-destination evidence: available simulator discovery, `id=<UDID>` destination selection, and boot/await before `xcodebuild test`.

## Verification

| Check | Status | Details |
|---|---|---|
| V21 scope proof | ✅ Pass | W39 brief states `V21 PASS on W37`. |
| Journey runner/CLI targeted suite | ✅ Pass | `pytest tests/test_journey_v2_runner.py tests/test_journey_v2_cli.py -q` -> 25 passed. |
| Python format | ✅ Pass | `ruff format validator/journeys/runner.py tests/test_journey_v2_runner.py` -> 2 files left unchanged. |
| Python format check | ✅ Pass | `ruff format --check validator/journeys/runner.py tests/test_journey_v2_runner.py` -> 2 files already formatted. |
| Python lint | ✅ Pass | `ruff check validator/journeys/runner.py tests/test_journey_v2_runner.py` -> pass. |
| Changed-file pyright | ✅ Pass | `pyright validator/journeys/runner.py tests/test_journey_v2_runner.py` -> 0 errors. |
| Diff hygiene | ✅ Pass | `git diff --check` -> pass. |
| LiveSpec coherence | ✅ Pass | `livespec validate --coherence` -> 0 errors, 0 warnings, 2 infos. |
| Visual gate | N/A | Feature 057 is a CLI/runtime feature; no UI baselines changed. |
| Full Strapt journey suite | Not run | Explicitly out of scope; no Strapt recompile, `$spec-test`, or journey run executed. |

## Remaining Gaps

No W38 documentation-scope finding remains. Strapt recompile, `$spec-test`, journey run, `$git-commit`, and push remain unexecuted and out of scope for this worker.

---

# Spec Fix Report - XCUITest Simulator Destination Resolution

**Checked:** 2026-06-09
**Feature:** `.specs/features/057-cross-feature-user-journeys-v2/`
**Command:** `$spec-fix "runtime journey runner uses unavailable iPhone 16 destination; resolve XCUITest simulator destination from available devices and ensure iOS/watchOS journeys boot real simulators before running"`
**Goal hash:** `2dc20b6bf9cdd20957aa042a055f5362c5a33b8687d2be8056a3f7a714e9e426`

## Verdict

**Overall health:** Fixed for the W31 runtime simulator destination failure.

The Strapt journey runtime failed because LiveSpec's compiled-only journey runner built XCUITest commands with the hardcoded default `platform=iOS Simulator,name=iPhone 16`. Current local simulators expose iPhone 17 and Apple Watch Series 11 devices, so `xcodebuild` could not launch a real simulator for the journey run.

## Gaps Closed

| Gap | Previous Status | Current Status | Evidence |
|---|---|---|---|
| GAP-015 - XCUITest journey runner hardcoded unavailable iPhone 16 | Blocking | ✅ Fixed | [`validator/journeys/runner.py`](../../../../validator/journeys/runner.py) removed `DEFAULT_XCODE_DESTINATION` and resolves available simulators via `xcrun simctl list devices available --json`. |
| GAP-016 - watchOS journeys used iOS fallback destination | Blocking | ✅ Fixed | Platform inference selects watchOS when the target/class name contains `watch`, and tests prove watch journeys use `platform=watchOS Simulator,id=<UDID>`. |
| GAP-017 - Journey runner did not boot selected simulator before XCUITest | Blocking | ✅ Fixed | Runner now calls `xcrun simctl boot <UDID>` and `xcrun simctl bootstatus <UDID> -b` before `xcodebuild test` when the resolved destination has `id=<UDID>`. |

## Fix Plan Executed

1. Add `simctl`-backed iOS/watchOS destination discovery.
2. Emit `id=<UDID>` Xcode destinations so the selected device is unambiguous.
3. Boot and await the resolved simulator before executing XCUITest journeys.
4. Add runner tests for unavailable `iPhone 16` → available iPhone selection, and watch journey → available Apple Watch selection.

## Verification

| Check | Status | Details |
|---|---|---|
| Journey runner/CLI targeted suite | ✅ Pass | `pytest tests/test_journey_v2_runner.py tests/test_journey_v2_cli.py -q` -> 14 passed. |
| Python lint | ✅ Pass | `ruff check validator/journeys/runner.py tests/test_journey_v2_runner.py` -> pass. |
| Python format | ✅ Pass | `ruff format --check validator/journeys/runner.py tests/test_journey_v2_runner.py` -> pass. |
| Changed-file pyright | ✅ Pass | `pyright validator/journeys/runner.py tests/test_journey_v2_runner.py` -> 0 errors. |
| Local simulator availability | ✅ Pass | `xcrun simctl list devices available --json` shows iOS `iPhone 17 Pro` UDID `8C365700-3496-4DEF-BCA8-6D6EFC6C5723` and watchOS `Apple Watch Series 11 (46mm)` UDID `659FC5B9-A363-45EF-84C0-23AD342BFEC9`. |
| Strapt destination resolution | ✅ Pass | `_runner_command()` against Strapt generated artifacts resolves iOS to `platform=iOS Simulator,id=8C365700-3496-4DEF-BCA8-6D6EFC6C5723` and watchOS to `platform=watchOS Simulator,id=659FC5B9-A363-45EF-84C0-23AD342BFEC9`; no `iPhone 16` destination emitted. |
| Full Strapt journey suite | Not run | Explicitly out of scope for W31; no `livespec journey run --all` or `$spec-test` retry executed. |

## Remaining Gaps

None in LiveSpec for the simulator destination failure. A later fresh worker must `$git-commit` LiveSpec, then a fresh Strapt worker must recompile journeys with the updated LiveSpec before retrying `$spec-test`.

---

# Spec Fix Report - Migration 20 Journey Recompile

**Checked:** 2026-06-09  
**Feature:** `.specs/features/057-cross-feature-user-journeys-v2/`  
**Command:** `$spec-fix 057-cross-feature-user-journeys-v2`  
**Goal hash:** `2dc20b6bf9cdd20957aa042a055f5362c5a33b8687d2be8056a3f7a714e9e426`

## Verdict

**Overall health:** Fixed for the Migration 20 downstream journey recompile gap.

Strapt migration evidence showed v20 only ran `migrate-agent-sync.sh` and `SET_VERSION 20`; no all-journey recompilation occurred, leaving existing `compiled/manifest.json` files at `compiler_version: journeys-v2-1`.

## Gaps Closed

| Gap | Previous Status | Current Status | Evidence |
|---|---|---|---|
| GAP-001 - Migration 20 did not invoke journey compilation | Blocking | ✅ Fixed | `migrations/20/migrate.md` now includes `RUN migrate-journeys-compile.sh` before `SET_VERSION 20`. |
| GAP-002 - Downstream projects could remain on `journeys-v2-1` manifests after v20 | Blocking | ✅ Fixed | `scripts/migrate-journeys-compile.sh` runs `livespec journey compile --force` from the migrated project root using the LiveSpec source tree being applied. |
| GAP-003 - No regression proving v20 rewrites old manifests | Blocking | ✅ Fixed | `tests/integration/test_migration_v19_user_journeys.py` creates an old `journeys-v2-1` manifest and verifies v20 rewrites it to the current compiler version with a native Playwright artifact. |

## Fix Plan Executed

1. Add a migration helper script that runs `PYTHONPATH="$LIVESPEC_DIR" python3 -m validator.cli journey compile --force` after `cd "$PROJECT_DIR"`.
2. Wire the helper into `migrations/20/migrate.md` between agent-sync refresh and `SET_VERSION 20`.
3. Extend the migration integration fixture to include a valid v2 journey with an old compiler manifest and assert the manifest/artifact are regenerated.

## Verification

| Check | Status | Details |
|---|---|---|
| Focused migration test | ✅ Pass | `pytest tests/integration/test_migration_v19_user_journeys.py -q` -> 2 passed. |
| Journey compiler/CLI regression suite | ✅ Pass | `pytest tests/test_journey_v2_compiler.py tests/test_journey_v2_cli.py tests/integration/test_migration_v19_user_journeys.py -q` -> 12 passed. |
| Python lint | ✅ Pass | `ruff check tests/integration/test_migration_v19_user_journeys.py` -> pass. |
| Python format | ✅ Pass | `ruff format --check tests/integration/test_migration_v19_user_journeys.py` -> pass. |
| Shell syntax | ✅ Pass | `bash -n scripts/migrate-journeys-compile.sh` -> pass. |
| Visual gate | N/A | Feature 057 is non-visual for LiveSpec runtime; no baselines updated. |

## Remaining Gaps

None for the Migration 20 force-recompile gap.

---

# Spec Fix Report - XCUITest Wait-Based Assertions

**Checked:** 2026-06-09
**Feature:** `.specs/features/057-cross-feature-user-journeys-v2/`
**Command:** `$spec-fix "LiveSpec XCUITest journey compiler must emit waitForExistence-based safe assertions for assert/assert_not instead of immediate .exists"`
**Goal hash:** `2dc20b6bf9cdd20957aa042a055f5362c5a33b8687d2be8056a3f7a714e9e426`

## Verdict

**Overall health:** Fixed for the XCUITest immediate assertion gap.

The Strapt W21 scan found 152 generated Swift assertions reading `.exists` immediately. The source defect was LiveSpec feature 057: `validator/journeys/compiler.py` rendered `XCTAssertTrue(... .exists)` / `XCTAssertFalse(... .exists)` directly for `assert` and `assert_not`.

## Gaps Closed

| Gap | Previous Status | Current Status | Evidence |
|---|---|---|---|
| GAP-012 - XCUITest `assert` read `.exists` immediately | Major | ✅ Fixed | `assert` now renders `assertJourneyElementExists(..., "<identifier>")`, and the helper calls `element.waitForExistence(timeout:)` before `XCTAssertTrue`. |
| GAP-013 - XCUITest `assert_not` read `.exists` immediately | Major | ✅ Fixed | `assert_not` now renders `assertJourneyElementDoesNotExist(..., "<identifier>")`, and the helper calls `element.waitForExistence(timeout:)` before `XCTAssertFalse`. |
| GAP-014 - Missing compiler regression for safe assertions | Major | ✅ Fixed | `tests/test_journey_v2_compiler.py` now proves helper generation, positive and negative assertion output, and absence of immediate descendant assertions. |

## Fix Plan Executed

1. Add XCUITest assertion timeout constants in [`validator/journeys/compiler.py`](../../../../validator/journeys/compiler.py).
2. Compile `assert` / `assert_not` to helper calls instead of direct `.exists` reads.
3. Generate Swift assertion helpers only for journeys that contain assertion actions.
4. Add compiler tests for positive and negative assertion rendering plus immediate `.exists` regression checks.

## Verification

| Check | Status | Details |
|---|---|---|
| Journey compiler targeted suite | ✅ Pass | `pytest tests/test_journey_v2_compiler.py -q` -> 15 passed. |
| Journey compiler/CLI/migration suite | ✅ Pass | `pytest tests/test_journey_v2_compiler.py tests/test_journey_v2_cli.py tests/integration/test_migration_v19_user_journeys.py -q` -> 20 passed. |
| Generated assertion scan | ✅ Pass | Generated Swift uses `waitForExistence(timeout:)`; compiler output no longer contains immediate `XCTAssertTrue/False(app.descendants(...).exists)`. |
| Python lint | ✅ Pass | `ruff check .` -> pass after line-length cleanup. |
| Python format | ✅ Pass | `ruff format --check .` -> pass. |
| Changed-file pyright | ✅ Pass | `pyright validator/journeys/compiler.py tests/test_journey_v2_compiler.py` -> 0 errors. |
| Changed-file mypy | ✅ Pass | `mypy validator/journeys/compiler.py tests/test_journey_v2_compiler.py` -> no issues in 2 source files. |
| Diff hygiene | ✅ Pass | `git diff --check` -> pass. |
| LiveSpec coherence | ✅ Pass | `livespec validate --coherence` -> 0 errors, 0 warnings, 2 infos. |
| LiveSpec doctor | ✅ Pass | `livespec doctor` -> OK. |
| Visual gate | N/A | Feature 057 is a CLI/runtime compiler feature; no visual baselines changed. |

## Remaining Gaps

Downstream Strapt journeys must be recompiled by a fresh worker against this LiveSpec compiler change.

---

# Spec Fix Report - Native Compiler Completeness

**Checked:** 2026-06-09  
**Feature:** `.specs/features/057-cross-feature-user-journeys-v2/`  
**Command:** `$spec-fix 057-cross-feature-user-journeys-v2`  
**Goal hash:** `2dc20b6bf9cdd20957aa042a055f5362c5a33b8687d2be8056a3f7a714e9e426`

## Verdict

**Overall health:** Fixed for the LiveSpec compiler findings surfaced by the Strapt audit.

The audit showed generated Swift and manifest outputs that were incomplete or formatting-blocking. The source defect is LiveSpec feature 057 because the compiler wrote native artifacts even when runner actions were unsupported.

## Gaps Closed

| Gap | Previous Status | Current Status | Evidence |
|---|---|---|---|
| GAP-004 - Unsupported native steps emitted as comments | Blocking | ✅ Fixed | `validator/journeys/capabilities.py` now rejects unsupported and malformed runner actions before output; `tests/test_journey_v2_compiler.py` proves XCUITest `wait` and malformed step payloads do not write Swift. |
| GAP-005 - Generic XCUITest `testJourney` names | Minor | ✅ Fixed | `validator/journeys/compiler.py` emits `test<JourneyId>Journey()`; compiler test asserts no generic `testJourney()`. |
| GAP-006 - XCUITest assertions used narrow/static text lookup | Major | ✅ Fixed | XCUITest target lookup uses `app.descendants(matching: .any)["<identifier>"]` for stable IDs and text fallbacks. |
| GAP-007 - Supported XCUITest fill/assert_not/open lacked concrete code | Blocking | ✅ Fixed | Compiler emits `typeText`, `XCTAssertFalse`, screenshot attachments, and `openJourneyURL(_:in:)` backed by `XCUIApplication.open(URL)`. Regression tests also prove journey preconditions become launch environment before `app.launch()`, and no generated XCUITest open path uses `Process()` or post-launch `launchEnvironment` mutation. |
| GAP-008 - Manifest JSON lacked trailing newline | Nit | ✅ Fixed | `write_compiled_manifest()` writes formatter-friendly JSON with a final newline; compiler test checks bytes end with `\n`. |
| GAP-009 - W14 audit required project-level verification docs | Major | ✅ Fixed | Changelog, implementation, and check artifacts now document full-project lint/format plus the honest type-check boundary: `mypy .` was rerun and remains red on 52 pre-existing errors outside the W13/W15 touched files, while `pyright` and targeted `mypy` pass on the touched compiler/test files. |
| GAP-010 - Source reread failure silently dropped preconditions | Major | ✅ Fixed | `compile_journeys()` now returns `journey_source_unreadable` before writing artifacts if the source cannot be loaded into `JourneySourceV2`; regression coverage deletes the source after validation and asserts no manifest is written. |
| GAP-011 - XCUITest auth/feature flags not asserted before launch | Major | ✅ Fixed | Compiler regression coverage now asserts `UI_TEST_JOURNEY_AUTH` and `UI_TEST_JOURNEY_FEATURE_FLAGS` are emitted before `app.launch()`. |

## Fix Plan Executed

1. Extend runner capability validation from runner-only checks to per-action checks before native artifact writes.
2. Generate concrete XCUITest code for supported actions: open, click, assert, assert_not, fill, screenshot, and back.
3. Keep unsupported condition-free waits, malformed step dictionaries, missing target/value payloads, and XCUITest non-URL `open` inputs out of generated native output by returning `journey_capability_unsupported`.
4. Add regression tests for unsupported-action rejection, malformed payload rejection, named XCUITest methods, concrete fill/assert_not/open/screenshot output, pre-launch fixtures/mocks injection, no `Process()` URL-open path, and manifest trailing newline.
5. Centralize XCUITest `UI_TEST_JOURNEY_*` environment keys and fail compilation explicitly with `journey_source_unreadable` when a source cannot be reread.
6. Upgrade W14 audit verification documentation to project-level `ruff check .` and `ruff format --check .`, then requalify the type gate honestly: `mypy .` is recorded as a pre-existing project-wide failure, while W13/W15 touched files pass targeted `mypy` and `pyright`.

## Verification

| Check | Status | Details |
|---|---|---|
| Journey compiler targeted suite | ✅ Pass | `pytest tests/test_journey_v2_compiler.py -q` -> 14 passed. |
| Journey compiler/CLI/migration suite | ✅ Pass | `pytest tests/test_journey_v2_compiler.py tests/test_journey_v2_cli.py tests/integration/test_migration_v19_user_journeys.py -q` -> 19 passed. |
| Python lint | ✅ Pass | `ruff check .` -> pass. |
| Python format | ✅ Pass | `ruff format --check .` -> pass. |
| Changed-file pyright | ✅ Pass | `pyright validator/journeys/compiler.py tests/test_journey_v2_compiler.py` -> 0 errors. |
| Changed-file mypy | ✅ Pass | `mypy validator/journeys/compiler.py tests/test_journey_v2_compiler.py` -> no issues in 2 source files. |
| Project mypy | ⚠️ Pre-existing project debt | `mypy .` -> 52 errors in 27 files, none in `validator/journeys/compiler.py` or `tests/test_journey_v2_compiler.py`; categories include missing third-party stubs (`yaml`, `frontmatter`, `claude_agent_sdk`) and older type issues in visual gate, schemas, doctor, mutation reports, and integration helpers. |
| Diff hygiene | ✅ Pass | `git diff --check` -> pass. |
| LiveSpec coherence | ✅ Pass | `livespec validate --coherence` -> 0 errors, 0 warnings, 2 infos. |
| LiveSpec doctor | ✅ Pass | `livespec doctor` -> OK. |
| Visual gate | N/A | Feature 057 is a CLI/runtime compiler feature; no visual baselines changed. |

## Remaining Gaps

Strapt must be recompiled by a fresh worker against the updated LiveSpec compiler. Strapt journey-source issues such as condition-based replacements for arbitrary waits are not edited in this worker because the brief requires PARTIAL after modifying LiveSpec.
