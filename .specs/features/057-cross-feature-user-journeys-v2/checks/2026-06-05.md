# Spec Fix Report - Cross-Feature User Journeys v2

**Checked:** 2026-06-05  
**Feature:** `.specs/features/057-cross-feature-user-journeys-v2/`  
**Command:** `$spec-fix 057-cross-feature-user-journeys-v2`  
**Goal hash:** `b9f03cf06801cd816205afdd35baa23998024a5e3bacb0f65dd4a679d47499da`

## Verdict

**Overall health:** Fixed for feature-scoped gaps from `checks/2026-06-04.md`.

The 2026-06-04 check found four blocking gaps and three convention gaps. This fix closes the feature-scoped gaps by adding canonical implementation mappings, updating stale journey workflow wording, enforcing compiled-only journey runs in the relevant skills, and tightening Python exception/convention handling.

## Gaps Closed

| Gap | Previous Status | Current Status | Evidence |
|---|---|---|---|
| GAP-001 - `implementation.md` missing canonical mapping | Blocking | ✅ Fixed | `implementation.md` now has `Requirement Mapping`, `Acceptance Criteria Mapping`, and `Files Created/Modified` sections covering FR-001..FR-041 and AC-001..AC-046. |
| GAP-002 - `$spec-test` scans v1 feature-scoped journey paths | Blocking | ✅ Fixed | `.agent-sync/skills/spec-test/SKILL.md` now uses global `.specs/journeys/<journey-id>/journey.yaml`; legacy `.journey.yaml` is marked migration-only. |
| GAP-003 - `$spec-feature` compiles journeys during test gate | Blocking | ✅ Fixed | `.agent-sync/skills/spec-feature/SKILL.md` now runs `livespec journey impact` + compiled-only `livespec journey run`; compile is limited to explicit create/edit/compile workflows. |
| GAP-004 - `$spec-specify` proposes v1 feature-scoped journey storage | Blocking | ✅ Fixed | `.agent-sync/skills/spec-specify/SKILL.md` now proposes global v2 journey IDs and delegates YAML writes to `$spec-journey create/edit`. |
| Convention - silent `except Exception` in `runner.py` | Gap | ✅ Fixed | `_read_source()` catches `OSError`, `yaml.YAMLError`, and `ValidationError` explicitly. |
| Convention - silent `except Exception` in `impact.py` | Gap | ✅ Fixed | `_read_source()` catches `OSError`, `yaml.YAMLError`, and `ValidationError` explicitly. |
| Convention - unexplained `type: ignore` / `cast()` | Warning | ✅ Fixed | PyYAML suppressions and JSON boundary casts now include rationale comments. |
| AC-020 evidence gap - narrow `JourneyImpactAnalyzer` | Partial | ✅ Fixed | Impact analysis now incorporates `SmartTestSelector` and stable target signals: `semantic_id`, `test_id`, `i18n_key`, `accessibility_label`, `route`, `label`, `role`, `name`, and product-contract text. |

## Verification

| Check | Status | Details |
|---|---|---|
| Targeted Journey v2 tests | ✅ Pass | `pytest tests/test_journey_v2_*.py -q` -> 38 passed. |
| Targeted ruff check | ✅ Pass | `ruff check validator/journeys validator/cli_commands/journey_cmd.py validator/cli_commands/test_cmd.py tests/test_journey_v2_impact.py` -> pass. |
| Targeted ruff format | ✅ Pass | `ruff format --check validator/journeys validator/cli_commands/journey_cmd.py validator/cli_commands/test_cmd.py tests/test_journey_v2_impact.py` -> 23 files already formatted. |
| Pyright | ✅ Pass | `pyright validator/` -> 0 errors. |
| Full non-integration suite | ✅ Pass | `pytest tests/ --ignore=tests/integration -q` -> 1585 passed, 4 skipped, 170 warnings. |
| Visual gate | N/A | Feature 057 is non-visual for LiveSpec runtime; no baselines updated. |

## Remaining Gaps

None for the gaps listed in `checks/2026-06-04.md`.

Project-wide pre-existing warnings remain outside feature 057: legacy feature directory naming (`005.1-*`), duplicate feature number `005`, missing number `012`, `.specs/features/.DS_Store`, and global doctor findings. They were not part of the feature-scoped fix.
