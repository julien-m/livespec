## Test Report: 036-multi-surface-detection-and-migration

**Date:** 2026-05-07
**Feature:** `.specs/features/036-multi-surface-detection-and-migration/`
**Mode:** full (--auto --update)

---

### AC Coverage

| AC | Description | Test | Result | Source | Notes |
|---|---|---|---|---|---|
| AC-001 | Split layout → two surfaces with correct ids/testDirs/configs | `tests/test_generate_surfaces.js` — "apps/* split layout emits 2 surfaces with correct ids" | ✅ Pass | Existing | Verifies id, testDir, runnerConfig for both entries |
| AC-002 | Generator emits [WARNING] with migrate-visual-tests.js hint | `tests/test_generate_surfaces.js` — same test; warnSpy asserts message contains "migrate-visual-tests.js" | ✅ Pass | Existing | console.warn spy confirms call and text |
| AC-003 | Only tests/e2e → exactly one surface (no regression) | `tests/test_generate_surfaces.js` — "regression: only tests/e2e → exactly 1 surface, no -visual suffix" + "returns 1 entry when only tests/e2e exists" | ✅ Pass | Existing | Two tests cover this independently |
| AC-004 | Only tests/visual → one surface with visual testDir and runnerConfig | `tests/test_generate_surfaces.js` — "returns 1 visual entry when only tests/visual exists" | ✅ Pass | Existing | Checks testDir and configFile fields |
| AC-005 | --migrate-surfaces appends missing visual entry; existing entries byte-for-byte identical | `tests/test_generate_surfaces.js` — "appends missing visual surface preserving existing entries byte-for-byte" + "preserves user-edited custom name field on additive migrate" | ✅ Pass | Existing | Uses `after.startsWith(before)` guard for byte-for-byte check |
| AC-006 | --migrate-surfaces idempotent when visual entry already present | `tests/test_generate_surfaces.js` — "idempotent: no-op when manifest already contains visual surface" | ✅ Pass | Existing | Second run produces identical file + "No new surfaces detected" message |
| AC-007 | --migrate-surfaces is additive-only (no --force required) | `tests/test_generate_surfaces.js` — "appends missing..." test uses no --force; "--migrate-surfaces + --force: full overwrite" tests the override path | ✅ Pass | Existing | Both additive path and force-override path explicitly tested |
| AC-008 | Monorepo: N apps each split → 2N surfaces in app-interleaved order | `tests/test_generate_surfaces.js` — "monorepo split: 2 apps each split → 4 surfaces in interleaved order" + "mixed monorepo" | ✅ Pass | Existing | Interleaving invariant: indexOf(web-visual) === indexOf(web)+1 |
| AC-009 | surfaces.yaml schema remains flat list — no new top-level keys | `tests/test_generate_surfaces.js` — all integration tests read emitted surfaces as flat list | ⚠️ Partial | Existing | No dedicated schema shape assertion; implicit coverage via structural assertions on surfaces array |
| AC-010 | Consolidated single-surface layout unaffected | `tests/test_generate_surfaces.js` — "regression: only tests/e2e → exactly 1 surface, no -visual suffix" | ✅ Pass | Existing | Explicit regression guard; asserts no "-visual" suffix in id |
| AC-011 | --dry-run works for multi-surface case | `tests/test_generate_surfaces.js` — "--dry-run prints multi-surface output for split layout" + "--migrate-surfaces + --dry-run does not write file" | ✅ Pass | Existing | File unchanged after dry-run; stdout contains both surface ids |
| AC-012 | id derivation: appdir + appdir-visual; collision → appdir-visual-v2 | `tests/test_generate_surfaces.js` — "collision: apps/web and apps/web-visual both exist → web visual surface uses -v2 suffix" | ✅ Pass | Existing | Verifies web-visual-v2 in ids; web and web-visual (for real app) also present |

**Coverage:** 11/12 fully covered (92%), 1 partial (AC-009 — implicit only)

---

### Suite Results

| Suite | Command | Result | Duration |
|---|---|---|---|
| JS unit (Bun) | `bun test ./tests/test_generate_surfaces.js` | ✅ 22/22 | ~422ms |
| Python unit | `pytest tests/ --ignore=tests/integration -v --tb=short` | ✅ 884/884 | ~4.73s |

**Total: 906 tests, 0 failures.**

---

### Visual Baselines

Not applicable — this feature has no `## Screens` section; it is a CLI/scripting feature only.

---

### Generation Summary

| Metric | Value |
|---|---|
| Tests generated | 0 |
| Tests already existing | 22 |
| Generation passed | n/a |
| Generation failed (impl bug) | 0 |
| Generation failed (compile) | 0 |

No tests were generated — all 22 JS tests were already created as part of the implementation (TDD). AC coverage is complete through existing tests.

---

### Summary

- **AC coverage:** 11/12 (92%) — 1 partial (AC-009: schema flatness tested implicitly, not via a dedicated assertion)
- **Test suite:** 22/22 JS tests + 884/884 Python tests (906 total) — all passing
- **Visual:** N/A (CLI feature)
- **Overall:** ✅ All tests passing — AC-009 gap is low-risk (flat schema is enforced implicitly by every test that reads the surfaces array)
