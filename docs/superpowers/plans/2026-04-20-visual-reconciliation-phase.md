# Visual Reconciliation Phase — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add an AI reconciliation phase (Step 4.6) to `commands/migrate.md` that detects and fixes visual test file issues after scaffolding.

**Architecture:** Single file modification — add prose instructions to `commands/migrate.md` for the agent to execute post-scaffolding verification and cleanup.

**Tech Stack:** Markdown (agent instructions)

---

## File Map
- Modify: `commands/migrate.md` — Update Step 4.5 point 6 to parse `routes=R` + add Step 4.6 reconciliation phase + update flowchart + update Step 5 report section

---

### Task 1: Update Step 4.5 sentinel parsing

**Files:**
- Modify: `commands/migrate.md:115-117`

- [ ] **Step 1: Read current Step 4.5 point 6**

The current text:
```markdown
6. **Parse sentinel from output:**
   Extract the `VISUAL_SCAFFOLD_RESULT: files=N dirs=M` line from `VISUAL_OUTPUT`.
   Store `FILES` and `DIRS` counts for display in Step 5.
```

- [ ] **Step 2: Update to parse all 3 fields**

Replace with:
```markdown
6. **Parse sentinel from output:**
   Extract the `VISUAL_SCAFFOLD_RESULT: files=N dirs=M routes=R [reason=...]` line from `VISUAL_OUTPUT`.
   Store `FILES`, `DIRS`, and `ROUTES` counts for display in Step 5.
   If `reason=no-frontend` is present, store `REASON` for the report.
```

- [ ] **Step 3: Commit**

```bash
git add commands/migrate.md
git commit -m "docs(migrate): parse routes field from visual scaffold sentinel"
```

---

### Task 2: Add Step 4.6 — Visual Test Reconciliation

**Files:**
- Modify: `commands/migrate.md` — insert new section between Step 4.5 and Step 5

- [ ] **Step 1: Read current file to identify insertion point**

Insert the new Step 4.6 section after line 119 (end of Step 4.5 point 7) and before line 121 (### Step 5 — Report).

- [ ] **Step 2: Insert Step 4.6 content**

Insert the following markdown block:

```markdown
### Step 4.6 — Visual Test Reconciliation (AI)

**Runs when:** Step 4.5 sentinel shows `FILES > 0` OR `ROUTES > 0`.
**Skip when:** `FILES == 0 AND ROUTES == 0`, or Step 4.5 was skipped/failed.

**Rollback boundary:** Before modifying any file, stage all files generated by Step 4.5:
```
git add <TEST_DIR>/
```
This creates a clean boundary — Step 4.6 corrections remain unstaged and can be reverted with `git checkout -- <TEST_DIR>/`.

**Procedure:** Read all `.spec.ts` files in the test directory (`frontend/tests/e2e/` or `tests/visual/`). For each file, apply the 5 checks below. Fix issues directly and log each correction.

#### Check 1: Duplicate route coverage

List all `.spec.ts` files. Extract the `ROUTE` constant from each. If two or more files target the same route:
- Keep the file with more test cases (count `test(` occurrences)
- Delete the other file(s)
- Log: `Duplicate removed: {deleted-file} (same route as {kept-file})`

#### Check 2: Syntax errors from merge

For each file created or modified by Step 4.5:
- Count opening `{` and closing `}` braces — they must be equal
- Check for consecutive `});` on adjacent lines (sign of double-close from legacy merge)
- If found: remove the orphan `});` and fix indentation
- Log: `Syntax fixed: {file} (removed orphan closing brace)`

#### Check 3: Dead code in preserved sections

For each file containing `// ── Preserved from`:
- **NEVER delete content with real logic** (assertions, page.goto, expect, locator calls)
- Delete ONLY empty stubs: `test("placeholder", async () => {});` or `test.describe.skip(...)` blocks with no assertions
- Log: `Dead stub removed: {file} (empty placeholder in Preserved section)`

#### Check 4: Orphaned route tests

Cross-reference `route-*.spec.ts` files with the project's route directory:
- Read the routes directory (e.g., `frontend/app/routes/`, `src/routes/`, `src/pages/`)
- If a `route-*.spec.ts` targets a route with no matching file: **warn but do NOT delete**
- Log: `⚠ Potentially orphaned: {file} (route {route} not found in project)`

#### Check 5: Slug/route/heading coherence

For each file, verify:
- `ROUTE` value is plausible for the file slug (e.g., `route-analytics.spec.ts` → `ROUTE` should contain `/analytics`)
- `HEADING` is not a generic placeholder like `"Page Title"`, `"Feature Name"`, or identical to the feature slug
- Log warnings only: `⚠ Check heading: {file} (HEADING="{value}" may need update)`

**Summary:** After all checks, count total fixes and warnings. Store counts for Step 5 report.
```

- [ ] **Step 3: Commit**

```bash
git add commands/migrate.md
git commit -m "docs(migrate): add Step 4.6 — AI visual test reconciliation phase"
```

---

### Task 3: Update flowchart and Step 5 report

**Files:**
- Modify: `commands/migrate.md` — update mermaid flowchart + Step 5 report section

- [ ] **Step 1: Update the mermaid flowchart**

Add the reconciliation node between VMIGRATE and DONE:

Change:
```
    VMIGRATE --> DONE["✅ Migration complete"]
```
To:
```
    VMIGRATE --> RECONCILE["Visual reconciliation\n(AI checks)"]
    RECONCILE --> DONE["✅ Migration complete"]
```

- [ ] **Step 2: Update Step 5 report to include reconciliation results**

After the visual scaffolding summary block, add:

```markdown
**Append reconciliation summary** (if Step 4.6 ran):

```
Visual test reconciliation:
  {FIXES} fix(es) applied, {WARNINGS} warning(s)
```

If `FIXES > 0`, list each fix:
```
Visual test reconciliation:
  ✓ 1 duplicate removed (not-found.spec.ts → covered by route-not-found.spec.ts)
  ✓ 5 syntax fixes (double }); in route-*.spec.ts)
  ✓ 5 dead stubs removed (placeholder tests)
  ⚠ 1 potentially orphaned route
  0 heading issues
```

If Step 4.6 was skipped (no changes from Step 4.5):
```
Visual test reconciliation: skipped (no new files)
```
```

- [ ] **Step 3: Commit**

```bash
git add commands/migrate.md
git commit -m "docs(migrate): update flowchart and report for Step 4.6 reconciliation"
```

---

### Task 4: Final verification

- [ ] **Step 1: Re-read the full commands/migrate.md**

Verify:
- Step 4.5 point 6 parses `routes=R` and `reason=...`
- Step 4.6 is between Step 4.5 and Step 5
- Flowchart includes reconciliation node
- Step 5 includes reconciliation summary
- All markdown is well-formed (no broken links, no unclosed code blocks)

- [ ] **Step 2: Verify the mermaid flowchart renders correctly**

Check that the flowchart has the correct node connections:
```
VMIGRATE → RECONCILE → DONE
```

- [ ] **Step 3: Run existing tests to ensure no regression**

```bash
python3 -m pytest tests/integration/test_migrate_visual.py -v --tb=short
```

Expected: all 31 tests pass (no code changes, only docs).
