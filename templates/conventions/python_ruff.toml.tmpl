# LiveSpec conventions template — generated by livespec conventions scaffold
# File line-count limit ({{max_file_lines}} lines) is enforced by the LiveSpec gate, not ruff line-length.

[tool.ruff.lint]
select = ["E", "F", "I", "PLR"]

[tool.ruff.lint.pylint]
max-statements = {{max_function_lines}}
